# Runtime dependencies for marrow_server (TD4000273, mirrors pyproject.toml
# [project].dependencies minus local marrow-common, per plan.md D-4).
# Snyk/pip-audit scan this file; Docker installs marrow_server/requirements.txt.
# Versions are ==-pinned to the Glama-verified 2026-09-25 build freeze so local,
# CI and Glama builds resolve identically (no floating rebuilds).
fastapi==0.141.1
uvicorn[standard]==0.54.0
mcp[cli]==1.30.0,<2.0
lancedb[pylance]==0.39.0
pyarrow==25.0.1
pydantic==2.13.5
python-dotenv==1.2.3
pyyaml==6.0.3
aiofiles==25.1.0
fastembed==0.8.1
packaging==26.3
