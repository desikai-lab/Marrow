# Runtime dependencies for marrow_server (TD4000273, mirrors pyproject.toml
# [project].dependencies minus local marrow-common, per plan.md D-4).
# Snyk/pip-audit scan this file; Docker installs marrow_server/requirements.lock.
fastapi>=0.115
uvicorn[standard]>=0.30
mcp[cli]>=1.26,<2.0
lancedb[pylance]>=0.13
pyarrow>=16
pydantic>=2.7
python-dotenv>=1.0
pyyaml>=6.0
aiofiles>=23.0
fastembed>=0.3
