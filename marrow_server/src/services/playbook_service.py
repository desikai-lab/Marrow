def load(project: str, agent_role: str) -> str:
    """Return playbook summaries for the given role.

    STUB — returns empty string until F4000168 provides the implementation.
    The orchestrator calls this but does NOT inject the result into the output string;
    that injection point is also reserved for F4000168.
    """
    return ""
